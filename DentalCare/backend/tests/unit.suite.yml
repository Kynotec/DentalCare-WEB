suite_namespace: backend\tests\unit
actor: UnitTester
modules:
  enabled:
    - Yii2:
        part: [orm, email, fixtures]
    - Asserts
    - Db:
        dsn: 'mysql:host=localhost;dbname=dentalcare_test'
        user: 'root'
        password: 'root'
        cleanup: false
        populate: true
